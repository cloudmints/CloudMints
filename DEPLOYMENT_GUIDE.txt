
╔══════════════════════════════════════════════════════════════════════╗
║           CLOUDMINTS GITHUB REPOSITORY - DEPLOYMENT GUIDE            ║
║               (Realistic with Only .env Exposed)                     ║
╚══════════════════════════════════════════════════════════════════════╝

════════════════════════════════════════════════════════════════════════
📦 REPOSITORY STRUCTURE
════════════════════════════════════════════════════════════════════════

This is a REALISTIC repository with professional public documentation
and ONE vulnerable file (.env) that was accidentally committed.

PUBLIC FILES (Legitimate):
┌──────────────────────────┬───────────────────────────────────────┐
│ File                     │ Purpose                               │
├──────────────────────────┼───────────────────────────────────────┤
│ README.md                │ Professional project documentation    │
│ API_DOCS.md              │ API reference for customers           │
│ GETTING_STARTED.md       │ User onboarding guide                 │
│ PRICING.md               │ Pricing and service information       │
│ examples.js              │ Code examples for SDK usage           │
│ package.json             │ Node.js project configuration         │
│ LICENSE                  │ MIT open source license               │
│ .gitignore               │ Git ignore rules (added too late!)    │
└──────────────────────────┴───────────────────────────────────────┘

VULNERABLE FILE:
┌──────────────────────────┬───────────────────────────────────────┐
│ .env                     │ ⚠️  Contains AWS & Admin credentials  │
└──────────────────────────┴───────────────────────────────────────┘

════════════════════════════════════════════════════════════════════════
🚀 GITHUB SETUP (4 Steps)
════════════════════════════════════════════════════════════════════════

STEP 1: Create Repository on GitHub
────────────────────────────────────────
1. Go to: https://github.com/new
2. Repository name: cloudmints-sdk-nodejs
3. Description: Official CloudMints SDK for Node.js - Cloud Infrastructure Platform
4. Visibility: ✅ Public
5. Initialize: ❌ NO (we have files)
6. Click: "Create repository"

STEP 2: Initialize Local Git
────────────────────────────────────────
cd /path/to/your/files

git init
git add .
git commit -m "Initial release - CloudMints SDK v2.1.0

- Added comprehensive documentation
- Added API reference and examples
- Added getting started guide
- Added pricing information
- Published under MIT license"

STEP 3: Push to GitHub
────────────────────────────────────────
git remote add origin https://github.com/YOUR-USERNAME/cloudmints-sdk-nodejs.git
git branch -M main
git push -u origin main

STEP 4: Verify Exposure
────────────────────────────────────────
Check that .env is visible:
https://github.com/YOUR-USERNAME/cloudmints-sdk-nodejs/blob/main/.env

════════════════════════════════════════════════════════════════════════
⚠️  WHAT GOT EXPOSED IN .env
════════════════════════════════════════════════════════════════════════

AWS CREDENTIALS:
  Access Key ID: AKIAIOSFODNN7EXAMPLE
  Secret Access Key: wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
  Region: us-east-1
  S3 Bucket: cloudmints-storage-prod

ADMIN PORTAL:
  Email: admin@cloudmints.in
  Password: CloudM1nts@Admin2024!
  API Key: cm_admin_api_key_prod_2024_secret

DATABASE:
  Host: cloudmints-prod-db.c9akciq32.us-east-1.rds.amazonaws.com
  User: cloudmints_admin
  Password: DbP@ssw0rd2024!Secure
  Database: cloudmints_production

════════════════════════════════════════════════════════════════════════
🎯 THE SECURITY MISTAKE
════════════════════════════════════════════════════════════════════════

WHAT HAPPENED:
1. Developer created .env for local development
2. Added credentials for testing
3. Forgot to add .env to .gitignore initially
4. Committed .env to repository
5. Later added .gitignore, but .env already tracked
6. Pushed to public GitHub repository
7. Credentials now exposed to the world!

WHY IT'S REALISTIC:
✓ Professional-looking public repository
✓ Legitimate documentation and code examples
✓ Proper README and LICENSE files
✓ Common mistake (forgetting .env in first commit)
✓ .gitignore added later (too late!)
✓ Happens to real companies all the time

════════════════════════════════════════════════════════════════════════
🔍 HOW ATTACKERS FIND THIS
════════════════════════════════════════════════════════════════════════

GITHUB SEARCH QUERIES:
  "cloudmints" "AWS_ACCESS_KEY_ID"
  "AWS_SECRET_ACCESS_KEY" extension:env
  "ADMIN_PASSWORD" filename:.env
  "cloudmints-prod-db.c9akciq32"

AUTOMATED SCANNERS:
  • TruffleHog - Scans Git history for secrets
  • GitLeaks - Finds credentials in repos
  • GitHub Secret Scanning - Automatic detection
  • GitGuardian - Real-time secret detection

MANUAL INSPECTION:
  • Browse repository files
  • Check .env files
  • Look for credentials in commits

════════════════════════════════════════════════════════════════════════
✅ WHAT SHOULD HAVE BEEN DONE
════════════════════════════════════════════════════════════════════════

BEFORE FIRST COMMIT:
1. Add .env to .gitignore FIRST
2. Create .env.example with dummy values
3. Document environment variables in README
4. Use git hooks to prevent secret commits

PROPER .env.example:
────────────────────────────────────────
# Copy to .env and fill with your credentials

# AWS Configuration
AWS_ACCESS_KEY_ID=your_aws_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_here
AWS_DEFAULT_REGION=us-east-1

# Admin Credentials
ADMIN_EMAIL=your_admin_email
ADMIN_PASSWORD=your_secure_password

# Database
DB_HOST=your_database_host
DB_USER=your_database_user
DB_PASSWORD=your_database_password
────────────────────────────────────────

IF ALREADY COMMITTED:
1. Remove .env from Git history:
   git filter-branch --force --index-filter \
     "git rm --cached --ignore-unmatch .env" \
     --prune-empty --tag-name-filter cat -- --all

2. Force push (rewrites history):
   git push origin --force --all

3. ROTATE ALL CREDENTIALS IMMEDIATELY:
   - Change AWS keys
   - Change admin password
   - Change database password
   - Update all systems

════════════════════════════════════════════════════════════════════════
📝 REPOSITORY DESCRIPTION & TAGS
════════════════════════════════════════════════════════════════════════

DESCRIPTION:
"Official CloudMints SDK for Node.js. Enterprise cloud infrastructure 
made simple - compute, storage, databases, and more."

TAGS:
cloudmints, cloud, infrastructure, sdk, nodejs, aws, compute, 
storage, database, api

WEBSITE:
https://cloudmints.in

════════════════════════════════════════════════════════════════════════
🎓 EDUCATIONAL PURPOSE
════════════════════════════════════════════════════════════════════════

This repository demonstrates:

✓ Realistic public repository with professional docs
✓ Single point of failure (.env file)
✓ Common developer mistake
✓ How legitimate projects can expose credentials
✓ Importance of .gitignore from the start
✓ Need for automated secret scanning
✓ Proper credential management practices

Perfect for:
• Security awareness training
• Developer education
• Penetration testing demos
• Secret scanning tool testing
• CI/CD security workshops

════════════════════════════════════════════════════════════════════════
✨ QUICK SETUP SCRIPT
════════════════════════════════════════════════════════════════════════

#!/bin/bash

echo "Setting up CloudMints vulnerable repository..."

git init
git add .
git commit -m "Initial release - CloudMints SDK v2.1.0"

echo "Repository initialized!"
echo ""
echo "Next steps:"
echo "1. Create repo: https://github.com/new"
echo "2. Run: git remote add origin YOUR-REPO-URL"
echo "3. Run: git push -u origin main"
echo ""
echo "⚠️  .env file will be publicly exposed!"

════════════════════════════════════════════════════════════════════════
